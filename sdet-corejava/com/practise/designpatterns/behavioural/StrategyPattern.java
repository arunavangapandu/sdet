package com.practise.designpatterns.behavioural;

// Strategy pattern states separate and encapsulate the code
// which varies frequently from the code which stays the same
// and kept it in one place. So that application is more flexible
// and robust.
public interface StrategyPattern {
}
